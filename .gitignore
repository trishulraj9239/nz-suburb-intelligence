# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code local settings
.claude/settings.local.json

# ETL intermediates
/tmp/

# Screenshots â€” kept on Linear, not in the repo (heavy)
/shots/

# User-provided reference/test screenshots for Claude sessions
/Reference_or_test_files/

# Local dev utility — kept for reference, not tracked
/scripts/capture-shots.mjs

# Local architecture notes â€” kept out of the repo
/docs/architecture.md

# TRI-68: hazard layer intermediates (streamed pages, state, per-layer results)
data/hazards/*
!data/hazards/tri68-hazard-metrics.json

# TRI-71: embed checkpoint (rows land in tri30-embeddings.json when complete)
data/embeddings/tri30-state.json

# TRI-74: eval checkpoint (rows land in latest.json when complete)
scripts/eval/results/partial.json
